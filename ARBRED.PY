1 from sklearn.tree import DecisionTreeClassifier 2 3
# Création et entrainement du modèle 4 tree_clf DecisionTreeClassifier() 5 tree_clf.fit(X_train, y_train) 6 y_pred_tree tree_clf.predict(X_test) 7 8 # Evaluation 9 accuracy_tree accuracy_score(y_test, y_pred_tree) 10 precision_tree precision_score(y_test, y_pred_tree, average='weighted') 11 recall_tree recall_score(y_test, y_pred_tree, average='weighted') 12 f1_tree f1_score(y_test, y_pred_tree, average='weighted') 13 14 print("Arbre de Décision :") 15 print(f"Précision (Accuracy): {accuracy_tree:.4f}") 16 print(f"Précision: {precision_tree:.4f)") 17 print (f"Rappel (Recall): (recall_tree:.4f}") 18 print(f"F1-Score: (f1_tree:.4f}") 19 print("\nRapport de Classification:") 20 print(classification_report (y_test, y_pred_tree)) 21 print("Matrice de Confusion :") 22 print(confusion_matrix(y_test, y_pred_tree))